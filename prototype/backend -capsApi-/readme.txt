The folder loaded_img will contain the images of the different 
axis (axial, sagital, coronal). Tree folders will appear there: axial_slices,
coronal_slices and sagital_slices each containing the images of the corresponding
axis.

The remaining folder (stl_files) holds the stl data obtain after processing the
images.